# Deep-research prompt — stage 12: OTel propagation & transport

Copy everything in the fenced block below into ChatGPT deep-research mode as a single prompt. The block is self-contained. When results come back, save them as `xai/research/12-otel-propagation-and-transport.md` with inline citations preserved. Note: a few claims here (whether a specific server continues an incoming `traceparent` and emits a correlated child span) are empirical and will still be confirmed by spike S3 against running code — the deep research should map the standards and documented behavior, and flag anything that needs live verification.

```
You are doing deep research to support a specific open-source engineering decision, not a general literature review. Read the context, then do the task.

CONTEXT — the project. `xai` (github.com/EthicalML/xai) is an open-source library from The Institute for Ethical AI & ML, first released in 2017 for tabular ML explainability/fairness, being revitalized in 2026 for agentic LLM systems. It is a provider-neutral analysis layer over agent traces (OpenTelemetry GenAI / OpenInference), with an optional layer F that instruments a self-hosted open-weights INFERENCE SERVER (vLLM, SGLang, Ollama/llama.cpp) to emit model-internal signals (logit-uncertainty, linear-probe readings) as OpenTelemetry spans. Firm non-goals: no trace store, dashboard, eval runner, or agent runtime; internal-state signals are separately-labeled evidence channels, never presented as faithful chain-of-thought.

CONTEXT — the architecture problem. In real deployments the model does NOT run inside the agent process; the agent (LangGraph/CrewAI/custom OpenAI-SDK loop) calls a separate inference server over an OpenAI-compatible HTTP endpoint. So the agent-side trace and the server-side internal-state signal are produced by two different processes and must be correlated into one trace. The intended mechanism is OpenTelemetry / W3C Trace Context propagation: the agent's request carries a `traceparent` header, and the server attaches its internal-state readings as a child span under the same trace. We also want ONE OTLP emitter with thin per-backend adapters so the spans land in whichever backend the user already runs (raw OpenTelemetry collector, Pydantic Logfire, or Langfuse).

TASK. Research, with current (2024–2026) standards and documented behavior:
1. W3C Trace Context (`traceparent`/`tracestate`) propagation over HTTP, and the OpenTelemetry GenAI semantic conventions for client/agent/tool/inference spans — what a server-side model span should look like and how it attaches to an agent's trace.
2. Whether the common OpenAI-compatible client paths propagate `traceparent` on the request, and whether/how popular agent frameworks (LangGraph, CrewAI, plain OpenAI Python SDK, LlamaIndex) inject trace context on outbound model calls. Note where auto-instrumentation (OpenLLMetry/OpenInference) does this for you vs where manual propagation is needed.
3. Whether popular inference servers — vLLM, SGLang, Ollama/llama.cpp, and NVIDIA Triton — read an incoming `traceparent`, continue the trace, and emit their own OpenTelemetry spans; and what extension points they expose for adding custom span attributes. Flag clearly what is documented vs what would need empirical verification.
4. OTLP ingestion of externally-produced spans by Pydantic Logfire and by Langfuse: do they accept spans on a trace they didn't originate, render cross-service traces correctly, and preserve custom attribute namespaces (e.g. an `xai.parametric.*` namespace)? What is the minimal difference between targeting raw OTLP, Logfire, and Langfuse — i.e. how thin can a per-backend adapter be?
5. Privacy/redaction: conventions for keeping internal-state attributes (probe scores, top-k attributions) small and safe, and never exporting raw activations.

OUTPUT. Structured markdown with inline citations to official specs, docs, and repositories at each claim. For each server/backend give a concrete "supported / partial / needs-verification" status for trace-context continuation and custom-attribute emission. Explicitly flag every claim that spike S3 (a real vLLM/SGLang plugin emitting a probe scalar into a traceparent-correlated span) should confirm. End with the recommended transport design: the canonical span shape for a model-internal signal, and how thin the Logfire/Langfuse adapters can be over a single OTLP emitter.
```
