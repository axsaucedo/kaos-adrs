# Blog series todos

- [ ] **Isolation note proposal (needs approval before writing).** From [multi-tenancy-isolation-dsn-and-rls](../impl/learnings/multi-tenancy-isolation-dsn-and-rls.md) (reader question from Alexander): propose whether and where the series gets an isolation note, likely part 2 (scopes) or part 3 (infrastructure). Content to frame: the two isolation boundaries are different mechanisms (cross-store = DSN/connection level, a control-plane dial from shared-instance-different-DB to separate-instance; intra-store = app-level `WHERE scope_key`, hardenable with RLS on the relational tiers only); the vector tier stays app-scoped because Mem0 owns the pgvector connection; RLS is recommended hardening, not shipped. Honesty requirement: the code trusts the `WHERE` today and there is no per-store table or collection namespacing.
